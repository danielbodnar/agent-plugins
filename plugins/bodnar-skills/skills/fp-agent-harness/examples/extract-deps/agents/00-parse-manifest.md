# parse-manifest

```json
{
  "name": "parse-manifest",
  "closure": {},
  "constraints": [
    "Read only the dependencies and devDependencies objects.",
    "Return one package name per line and nothing else.",
    "If the input is not valid JSON, return the single line ERROR."
  ],
  "schema": "text"
}
```

List every package name from this manifest.

{{input}}
