# render-summary

```json
{
  "name": "render-summary",
  "closure": {},
  "constraints": [
    "Write at most four lines.",
    "State the runtime count and the tooling count.",
    "Name the heaviest runtime dependencies when any stand out."
  ],
  "schema": "text"
}
```

Summarize this dependency classification for a code reviewer.

{{input}}
