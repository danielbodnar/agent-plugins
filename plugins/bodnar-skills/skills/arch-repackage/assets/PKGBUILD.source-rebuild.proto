# Maintainer: arch-repackage <nobody@localhost>
#
# Source rebuild scaffold generated by arch-repackage.
#
# This is a STARTING POINT, not a finished PKGBUILD. arch-repackage cannot
# know how upstream builds this software. Fill in the TODO sections:
#   - source=()      real upstream tarball / VCS URL
#   - *sums=()       run `updpkgsums` once source= is correct
#   - makedepends=() build-time dependencies
#   - build()        the actual build invocation
#   - package()      install into "$pkgdir" with the right prefix
#
# Until then this package will not build.

pkgname=%PKGNAME%
pkgver=%PKGVER%
pkgrel=%PKGREL%
%EPOCH%
pkgdesc="%PKGDESC%"
arch=(%ARCH%)
url="%URL%"
license=(%LICENSE%)
depends=(%DEPENDS%)
makedepends=()  # TODO: build-time dependencies
source=()       # TODO: upstream source URL(s)
b2sums=()       # TODO: run `updpkgsums` after setting source=

build() {
  cd "$srcdir"
  # TODO: real build steps, e.g.
  #   ./configure --prefix=/usr
  #   make
  :
}

package() {
  cd "$srcdir"
  # TODO: install into the package root, e.g.
  #   make DESTDIR="$pkgdir" install
  :
}
