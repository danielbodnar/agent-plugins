# Maintainer: arch-repackage <nobody@localhost>
#
# Meta-package generated by arch-repackage. It ships no files; its only job is
# to pull in a set of dependencies so the layer can be installed as a unit.
# Edit depends=() to refine the layer. Do not add a payload here: if you need
# to ship files, that belongs in a leaf package.

pkgname=%PKGNAME%
pkgver=%PKGVER%
pkgrel=%PKGREL%
pkgdesc="%PKGDESC%"
arch=('any')
url="%URL%"
license=(%LICENSE%)
depends=(%DEPENDS%)

package() {
  # Meta-package: intentionally empty.
  :
}
