// astro.config.mjs: Astro Starlight configuration for an engineering handbook.
//
// To use:
//   1. bun create astro@latest -- --template starlight
//   2. Move handbook/*.md into src/content/docs/ (or adjust srcDir below).
//   3. Replace the generated astro.config.mjs with this file.
//   4. Run: bun astro dev (preview at http://localhost:4321)
//   5. Run: bun astro build (produces dist/ for deployment)

import { defineConfig } from 'astro/config'
import starlight from '@astrojs/starlight'

export default defineConfig({
  integrations: [
    starlight({
      title: 'Engineering Handbook',
      description: 'How we work',

      // Default to dark mode; users can toggle.
      defaultLocale: 'en',

      // GitHub edit links at the bottom of each page.
      editLink: {
        baseUrl: 'https://github.com/your-org/your-repo/edit/main/',
      },

      // Source-control link in the header.
      social: {
        github: 'https://github.com/your-org/your-repo',
      },

      // Sidebar autogenerated per directory. Each directory's pages appear
      // in alphabetical order, which is why the skeleton uses numeric prefixes.
      sidebar: [
        {
          label: 'Direction & Culture',
          autogenerate: { directory: '01-direction-and-culture' },
        },
        {
          label: 'Workflows',
          autogenerate: { directory: '02-workflows' },
        },
        {
          label: 'Planning',
          autogenerate: { directory: '03-planning' },
        },
        {
          label: 'Incident Management',
          autogenerate: { directory: '04-incident-management' },
        },
        {
          label: 'Monitoring & Quality',
          autogenerate: { directory: '05-monitoring-and-quality' },
        },
      ],

      // Customize the look. Pagefind search is built-in.
      customCss: [
        // Optional: add a CSS file at src/styles/custom.css for branding.
        // './src/styles/custom.css',
      ],
    }),
  ],

  // Cloudflare Workers deployment integration. Comment out if deploying
  // elsewhere.
  // adapter: cloudflare({ mode: 'directory' }),
})
